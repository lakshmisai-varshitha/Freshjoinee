<% if (@in).nil? %>

    <%else %>
      <% Array(@in).each do |ins| %>
        ins:<%=link_to ins.id,finances_editinsurance_path(ins.id)%>
        relation type:<%=ins.relation_type %><br/>
        name :<%=ins.name %><br/>
        dob:<%=ins.dob %><br/>
        gender:<%=ins.gender %><br/>

      <%end %>
    <%end %>





 <%#Pf.find_all_by_company_name_and_empid(current_user.company_name,current_user.empid)%>